# Examining Core State at Runtime Using GDB

!!! note "Stub"
    This page is a placeholder. It will describe how to run a simulation
    under host `gdb`, set breakpoints, and examine the simulated core's
    registers and memory at runtime, as an alternative to enabling Sitar's
    own logging (see [Installation](installation.md)). Not yet populated.
