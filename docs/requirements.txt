mkdocs==1.6.1
mkdocs-get-deps==0.2.0
mkdocs-material==9.7.1
mkdocs-material-extensions==1.3.1
pymdown-extensions==10.20.1
Pygments==2.19.2

# Custom Pygments lexers that provide syntax highlighting for Sitar code
# blocks (```sitar ... ```) and SPARC V8 assembly code blocks
# (```sparc ... ```). Vendored into this repo (see
# sitar_pygments_lexer/README.md and sparc_pygments_lexer/README.md) so
# this file is self-contained. Paths are relative to this requirements
# file -- install from the docs/ directory:
#     pip install -r requirements.txt
-e ./sitar_pygments_lexer
-e ./sparc_pygments_lexer
